name: flutter_applicatio_todo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0
  intl: ^0.18.1 
  sqflite: ^2.3.0   # ✅ SQLite database
  path: ^1.8.3      # ✅ For joining database path

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
